<section class="callouts">
    <div class="content-wrapper">
        <div class="columns is-multiline">
        {% for callout in page.header.callouts %}
            <div class="column callout is-4-tablet">
                <div class="callout-container">
                    <h2>{{ callout.title }}</h2>
                    <p>{{ callout.description }}</p>
                </div>
            </div>
        {% endfor %}
        </div>
    </div>
</section>