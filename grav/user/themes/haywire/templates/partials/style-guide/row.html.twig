<div class="columns">
    {% for style in styles %}
        <div class="column {{ columnClass | default('is-3') }}">
            {% include "partials/style-guide/swatch.html.twig" with {
            cardClass: cardClass,
            swatchType: swatchType,
            style: style
            } only %}
        </div>
    {% endfor %}
</div>
